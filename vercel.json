{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "buildCommand": "cd frontend && npm install && npm install -g typescript && npm run build",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "github": {
    "silent": true
  }
} 